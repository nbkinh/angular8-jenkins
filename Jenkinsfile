pipeline {
    agent { dockerfile true }
    
    stages {
        stage('Pre-build cleanup') {
            steps {
              echo myVar = $myName
            }
        }

        
    }
        

    
}
